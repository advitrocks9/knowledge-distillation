[project]
name = "kd-coder"
version = "0.1.0"
description = "Forward vs reverse KL distillation on Qwen2.5-Coder"
requires-python = ">=3.12"
dependencies = [
    "torch==2.11.0",
    "transformers>=4.43",
    "datasets>=2.21",
    "accelerate>=0.34",
    "tqdm>=4.66",
    "numpy>=1.26",
]
